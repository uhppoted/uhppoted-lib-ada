name: docgen

on:
  workflow_dispatch:

jobs:

  build:
    name: docgen
    runs-on: ubuntu-latest
    steps:

    - name: Check out code
      uses: actions/checkout@v6

    - name: Setup Alire
      uses: alire-project/setup-alire@v5

    - name: Setup gnatdoc
      uses: alire-project/alr-install@v2
      with:
        crates: gnatdoc

    - name: Generate doc
      run: gnatdoc --help

    # - name: Install gnatdoc
    #   run: |
    #     alr get gnatdoc
    #     cd gnatdoc_* && alr build
    #     echo "$PWD/bin" >> $GITHUB_PATH

    # - name: Install Ada tools
    #   run: |
    #     cd lib
    #     alr toolchain --install gnatdoc
    #     alr toolchain
    #     alr version
    #     alr install --info

    # - name: Generate doc
    #   run: |
    #     ls -la /home/runner
    #     find /home/runner -name "gnatdoc" -type f
    # #     # cd lib
    # #     # alr exec -- gnatdoc -P uhppoted_lib_ada.gpr
